# Project memory draft format

Return the candidate as one fenced Markdown block with these concise sections when applicable:

- project purpose and technology stack;
- verified development commands;
- architecture and important module boundaries;
- testing strategy and completion checks;
- platform or safety constraints;
- facts still requiring confirmation.

Head the candidate `# CODING_AGENT.md draft`. After the fence, remind the user that `/init` changed no files and that saving requires a separate explicit request.
